function addToCart(flavorName){
    // Add the flavor to the cart
    // You can use a data structure like an array or a Map to store the cart items
    // Update the total price and display it
    console.log(`Added ${flavorName} to the cart.`);
    // Implement the logic to update the total price and display it
    // For example, you can calculate the total price by adding the price of each flavor to the current total
    // Then, update the HTML element with the new total price
    // You can also add a "Remove" button next to each flavor in the cart to allow users to remove items from the cart
    // Implement the logic to remove the flavor from the cart and update the total price and display it
    // For example, you can remove the flavor from the array or Map and update the total price accordingly
    // Finally, display the updated cart items and total price
    // You can also implement a "Checkout" button to trigger the checkout process
    // Implement the logic to validate the cart items and collect payment details
    // Then, process the payment and display a success message or an error message if necessary
    
}